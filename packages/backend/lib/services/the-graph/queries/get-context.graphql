query GetContext($contextId: ID!, $chainNames: [String!]!) {
  crossContext(contextId: $contextId, chainNames: $chainNames) {
    chainName
    delegationSets {
      fromAccount {
        id
      }
      delegations {
        toAccount {
          id
        }
        numerator
      }
      expireTimestamp
      fromTimestamp
    }
    optouts {
      account {
        id
      }
      status
      fromTimestamp
    }
  }
}
